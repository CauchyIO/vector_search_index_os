resources:
  jobs:
    load_worldbank_metadata:
      name: load_worldbank_metadata
      tasks:
        - task_key: load_metadata
          notebook_task:
            notebook_path: ../src/open_data_etl/load_worldbank_metadata.ipynb
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "1"
